
<%= form_tag pet_path(@pet), method: "put" do %>
    <% if @pet.user == current_user %>
 <%= render partial: 'form', locals: {pet: @pet}, post: Pet.new %>
 <%else%>
 <%= render partial: 'adoption_form', locals: {pet: @pet}, post: Pet.new %>
 <%end%>
<% end %>
