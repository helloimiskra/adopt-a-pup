
<%= render partial: "pet_profile", locals: {pet: @pet} %>
<br>
<% if logged_in? && current_user.shelters.include?(@pet.shelter) %>
Click to edit <%= link_to @pet.name, edit_pet_path(@pet)%>'s page.
<%end%>
<br>
Click <%=link_to 'here', pets_path%> to go back.
<br>
Click <%=link_to 'here', '/'%> to go to the home page.