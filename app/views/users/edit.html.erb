
<% if @user == current_user %>

<%= form_tag user_path(@user), method: "put" do %>
 <%= render partial: 'form', locals: {user: @user}, post: User.new %>
<% end %>

<%else%>
Sorry, you do not have access to this page. Please <%=link_to 'click', pets_path%> to view all available pets. 
<%end%>
